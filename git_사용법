1. pwd(print working directory)
   - 현재 작업하는 폴더를 보여달라는뜻

2. ls(list)
   - 현재 내가 있는곳의 폴더/파일 을 띄워줌

3. ls -a(list all)
   - 숨겨진파일(.으로 시작하는 파일들)도 모두 볼수있음

4. cd 폴더명(change directory)
   - ls명령어에서 확인된 폴더로 이동가능
   - .. > 한단계 위의 폴더라는뜻
   - 폴더명/폴더명 >한번에 더 깊이 들어갈수 있음

5. mkdir 폴더명(make directory)
   - 폴더 만들기

6. touch 파일명
   -현재경로에서 파일을 생성하는명령어
   -파일의 생성과 파일의 날짜, 시간을 변경하는 명령어


1. git init(초기 세팅)
   - 프로젝트 시작전 한번만 입력한다
   - 정확한 프로젝트 폴더(경로)에서 입력해야함

2. git add&commit(코드 저장 명령어)
   - git add 파일명 > 저장할 파일을 지정
   - git commit -m "메세지 작성" > 저장
   - git config --global user.name 유저네임
   - git status > 코드에 변경이 있으면 빨간색이 뜸
   - git add . > 내 프로젝트의 변경사항을 한번에 지정할 수 있음 

3. git log(저장 내역을 확인)
   - 메세지를 잘 쓰는게 중요
   - q로 빠져나오기

4. git push
   - 코드수정>코드저장(add&commit)>
   - git push -u origin main : git push만 입력해도 git push origin main 으로 해줘

5. git clone
   -깃허브 레파지토리 코드 복사
   - git clone 주소 .(폴더없으면 .지우기)
   - .을 안치면 폴더가 새로생성

6. git pull(팀원이 코드변경후 저장)
   - push 했는데 안되면 pull 하기
   -  pull 했는데 안되면 hint 맨위에꺼 복붙